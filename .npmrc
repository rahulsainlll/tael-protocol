# Workspace packages are always linked from source, never fetched from a registry.
link-workspace-packages=true
prefer-workspace-packages=true

# Fail fast when a peer dependency is missing instead of silently ignoring it.
strict-peer-dependencies=false
auto-install-peers=true

# Deterministic, CI-friendly installs.
save-exact=false
